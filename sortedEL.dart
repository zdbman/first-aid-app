/*import 'package:first_aid_app/number.dart';

import 'main.dart';

List sortedLst() {
  List<sorted> lst_sorted = [];
  return lst_sorted;
}

class sorted {
  var City;
  var ZipCode;
  var Name;
  var Num;
  var Address;
  var Lat;
  var Long;
  var dist;

  sorted(
      {this.City,
      this.ZipCode,
      this.Name,
      this.Num,
      this.Address,
      this.Lat,
      this.Long,
      this.dist});
}

void sort1() {
  for (int i = 0; i < test().length; i++) {
    var temp = test()[i];
    sortedLst()[i] = sorted(
      test()[i]
    );
  }
}
*/